# Changelog

## 0.1.0

- Twig composition extractor (`extends`, `include` / `embed`, `import` /
  `from` / `use`, `{% block %}`).
- Idempotent merge into `graphify-out/graph.json` via `_origin`.
- CLI `graphify-postpass` / `python -m graphify_postpass`.
