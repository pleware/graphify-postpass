[project]
name = "graphify-postpass"
version = "0.1.0"
description = "House extractors that run after Graphify rebuilds graph.json."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "PWare", email = "pawel@pware.ai" }]
dependencies = []

[project.urls]
Homepage = "https://github.com/pleware/graphify-postpass"
Repository = "https://github.com/pleware/graphify-postpass"

[project.scripts]
graphify-postpass = "graphify_postpass.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[tool.hatch.build.targets.wheel]
packages = ["src/graphify_postpass"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
